{
  "name": "al-triangulation",
  "version": "1.0.0",
  "description": "A few functions for preparing triangulation of Adventure Land (code MMORPG) map data.",
  "main": "dist/main.js",
  "types": "dist/main.d.ts",
  "scripts": {
    "build-tsc": "run-s clean-ts tsc",
    "tsc": "tsc",
    "clean-ts": "del-cli dist",
    "typecheck": "tsc --noEmit",
    "build-bundle": "run-s clean-bundle bundle",
    "bundle": "webpack",
    "clean-bundle": "del-cli bundle",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "dependencies": {},
  "devDependencies": {
    "@types/node": "^14.14.8",
    "del-cli": "^3.0.1",
    "npm-run-all": "^4.1.5",
    "ts-loader": "^8.0.11",
    "typescript": "^4.1.2",
    "webpack": "^5.5.1",
    "webpack-cli": "^4.2.0"
  }
}
